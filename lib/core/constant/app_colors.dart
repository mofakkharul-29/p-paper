import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // prevent instantiation

  // ── Screen ───────────────────────────────────────────
  static const Color screenBackground = Color(0xFFB8BEC5);

  // ── Card ─────────────────────────────────────────────
  static const Color cardSurface = Color(0xFFFFFFFF);
  static const Color cardShadow = Color(0xFF1A2B4A);

  // ── Section badge ────────────────────────────────────
  static const Color badgeBackground = Color(0xFFEEF3FF);
  static const Color badgeText = Color(0xFF2A52BE);

  // ── Typography ───────────────────────────────────────
  static const Color dateText = Color(0xFF8A94A6);
  static const Color titleText = Color(0xFF111827);
  static const Color bodyText = Color(0xFF4B5563);

  // ── Placeholder ──────────────────────────────────────
  static const Color placeholderBg = Color(0xFFE8ECF2);
  static const Color placeholderIcon = Color(0xFFB0BAC9);

  // ── Indicators / interactive ─────────────────────────
  static const Color spinnerColor = Color(0xFF2A52BE);
  static const Color refreshBackground = Color(0xFF1A2B4A);
  static const Color refreshForeground = Color(0xFFFFFFFF);

  // __ appbar color ______________________________
  static const Color appBarBgColor = Color(0xFFB6B4B4);
}
